// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add this if you're having middleware issues
  experimental: {
    // Remove if causing issues
  }
};

module.exports = nextConfig;
